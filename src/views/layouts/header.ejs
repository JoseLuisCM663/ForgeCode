<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forge Code Studio</title>
  <link rel="stylesheet" href="/css/style.home.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
  </style>
</head>
<body>
  <header>
    <nav>
      <span><a href="/">Forge Code Studio</a> </span>
      <div>
        <a href="/servicios">Servicios</a>
        <a href="/industria">Industrias</a>
        <a href="/nosotros">Nosotros</a>
        <a href="/desarrolladores">Desarrolladores</a>
        <a href="/buzon/contacto">Contactanos</a>
        <% if (typeof ID === 'number') { %>
            <a id="logout">Salir</a>
            <a href="/users/profile">
                <i class="bi bi-person-circle"></i> <%= mail %>
            </a>
                    <% } else { %>
            <a href="/users/register">Registrarse</a>
            <a href="/users/login">Iniciar sesión</a>
            <% } %>
      </div>
    </nav>
  </header>
  <script>
    const logout = document.getElementById('logout');
    if (logout) {
        logout.addEventListener('click', () => {
            fetch('/users/logout', {
                method: 'POST',
            }).then(() => {
                window.location.href = '/';
            });
        });
    }
  </script>